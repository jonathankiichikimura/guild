<% content_for :title, "Quests — Guild" %>

<div class="quests-page container-fluid py-4 px-4 px-md-5">
  <% if current_giver %>
    <%= render "giver_index" %>
  <% else %>
    <%# wont render anything ATM %>
    <div id="root" data-quests="<%= @quests.to_json %>"></div>
  <% end %>
</div>
