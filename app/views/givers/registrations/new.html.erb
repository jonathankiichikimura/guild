<div class="container py-6">
  <div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">

      <div class="text-center mb-4">
        <div class="step-icon-wrap mb-3">
          <i class="fa-solid fa-heart"></i>
        </div>
        <h2 class="section-title">Join as a Quest Giver</h2>
        <p class="section-subtitle">Post tasks you'd like done and get reliable help from caring community members nearby.</p>
      </div>

      <div class="for-you-card for-you-giver p-4 p-md-5">
        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= f.error_notification %>

          <div data-controller="onboarding">
            <p data-onboarding-target="progress"></p>

            <div data-onboarding-target="step">
              <%= f.input :first_name,
                          required: true,
                          autofocus: true,
                          input_html: { autocomplete: "given-name" } %>
              <%= f.input :email,
                          required: true,
                          input_html: { autocomplete: "email" } %>
              <%= f.input :password,
                          required: true,
                          hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                          input_html: { autocomplete: "new-password" } %>
              <%= f.input :password_confirmation,
                          required: true,
                          input_html: { autocomplete: "new-password" } %>

              <div class="d-flex justify-content-end mt-3">
                <span data-onboarding-target="nextBtn">
                  <button type="button"
                          data-action="onboarding#advance"
                          data-onboarding-step-param="+1"
                          class="btn btn-mustard">Next</button>
                </span>
              </div>
            </div>

            <div data-onboarding-target="step">
              <%= f.input :suburb,
                          required: true,
                          collection: TokyoWard::WARDS,
                          include_blank: "Select your ward",
                          hint: "The area where you live — shown publicly on your quests" %>
              <%= f.input :emergency_contact_name,
                          required: true,
                          label: "Emergency contact name" %>
              <%= f.input :emergency_contact_phone,
                          required: true,
                          label: "Emergency contact phone" %>

              <div class="mt-2">
                <%= f.button :submit, "Post Your First Quest", class: "btn btn-imperial w-100" %>
              </div>

              <div class="d-flex mt-3">
                <span data-onboarding-target="backBtn">
                  <button type="button"
                          data-action="onboarding#advance"
                          data-onboarding-step-param="-1"
                          class="btn btn-outline-secondary">Back</button>
                </span>
              </div>
            </div>
          </div>
        <% end %>
      </div>

      <div class="text-center mt-3">
        <%= render "givers/shared/links" %>
      </div>

    </div>
  </div>
</div>
